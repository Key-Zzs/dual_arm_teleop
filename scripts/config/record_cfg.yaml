record:
  # repo_id: &repo_id "franka_dual_arm/left_right_pick_chp_rollout"
  repo_id: &repo_id "nero_task3_step1_random/empty"
  # Suggested format: <robot_task<num>_step<num>>/<description>, e.g. nero_task3_step1/2mL_empty
  # <description>: https://github.com/wbcdcompetition/wbcd_tasks/tree/main/track-3-lab-experiments
  #   step1:
  #     2mL_empty: Seat a 2 mL vial into the 2 mL empty rack
  #     2ml_filled: Seat a filled 2 mL vial into the 2 mL filled rack
  #     8mL_empty: Seat an 8 mL vial into the 8 mL empty rack
  #     8mL_filled: Seat a filled 8 mL vial into the 8 mL filled rack
  #   step2 opc
  #   step3:
  #     remove_cap: Remove the cap from the source bottle
  #     remove_tubing: Remove the tubing from the source bottle
  #     insert_tubing: Insert the tubing into the target bottle
  #     replace_cap: Replace the cap on the source bottle

  debug:
    robot_debug: false # if true, robot will not execute arm actions, but gripper can still be controlled
  user_notes:  # any additional notes about the dataset
  fps: 30 # dataset and camera frame rate
  run_mode: "run_policy" # run_record | run_policy | run_mix

  # Robot type selection. Supported robots: dobot_dual_arm, franka_dual_arm, nero_dual_arm, arx_dual_arm, franka.
  # Hardware-specific data acquisition / teleop / robot / cameras sections are loaded from scripts/config/DAQ_config by robot_type.
  # robot_type: &robot_type "franka_dual_arm"
  robot_type: &robot_type "nero_dual_arm"

  # Optional explicit override if you do not want the robot_type default mapping:
  # daq_config_path: scripts/config/DAQ_config/franka_teleop.yaml
  # daq_config_path: scripts/config/DAQ_config/nero_teleop.yaml

  policy:
    # 策略类型: act | diffusion | dp | diffusion_policy
    # 轮次式 DAgger 的 run_mix 采集目前只实现 ACT checkpoint 推理。

    # Franka:
    # For ACT:
    # type: "act"
    # config_path: scripts/config/policy_config/act_reason_config.yaml
    # pretrained_path: "/home/geist/wbcd_workspace/lerobot/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/task3_step1/empty/act_20260529_E113_phase/checkpoints/last/pretrained_model"
    # For diffusion:
    # type: "diffusion"
    # config_path: scripts/config/policy_config/diffusion_reason_config.yaml
    # pretrained_path: "/home/deepcybo/Le-nero/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/franka_dual_arm/dp_left_right_pick_20260526_E150/checkpoints/last/pretrained_model"

    # Nero:
    # For ACT:
    type: "act"
    config_path: scripts/config/policy_config/act_reason_config.yaml
    pretrained_path: "/home/deepcybo/wbcd_ws/Le-nero/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/task3_step1_random/dual_empty/act_20260603_E200/checkpoints/last/pretrained_model"
    # For diffusion:
    # type: "diffusion"
    # config_path: scripts/config/policy_config/diffusion_reason_config.yaml
    # # pretrained_path: "/home/deepcybo/wbcd_ws/Le-nero/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/task3_step1/dual_empty/dp_20260601_E100_cleaned/checkpoints/last/pretrained_model"
    # # pretrained_path: "/home/deepcybo/wbcd_ws/Le-nero/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/task3_step1/empty/dp_20260530_E113_phase/checkpoints/last/pretrained_model"
    # pretrained_path: "/home/deepcybo/wbcd_ws/Le-nero/dual_arm_data_collection/lerobot_dual_arm_teleop/outputs/train/task3_step1_random/2mL_empty_right/dp_20260603_E125/checkpoints/last/pretrained_model"

    # Policy hyperparameters have been moved to scripts/config/policy_config/*_reason_config.yaml.

  task:
    description: "pick up small vials and insert them in the empty rack" # task description
    num_episodes: 50 # number of episodes to record
    display: true # whether to display data during recording
    resume: false # whether to resume from an existing dataset
    resume_dataset: # dataset to resume from if resume is true
    # robot-dagger sets success_policy automatically for full_success_episode/hybrid.
    # explicit prompts y/n; recorded_is_success saves every kept episode as success=true.
    success_policy:
    annotate_success: false # ask after each run_mix episode and write a per-frame success flag

  time:
    episode_time_sec: 6000 # max time per episode in seconds
    reset_time_sec: 6000 # max time to reset between episodes in seconds
    save_meta_period: 1 # save metadata after every N episodes

  storage:
    push_to_hub: false # whether to push the dataset to your HuggingFace repo after recording

replay:
  dataset_name: nero_task3_step1/empty_merged_E113_cleaned_mirrored
  episode_idx: 0 # episode index to replay
  reset_on_finish: true # replay结束后回home
  disconnect_on_finish: false # 默认不断开，避免触发server stop/e-stop
  robot_type: *robot_type # robot details are loaded from scripts/config/DAQ_config by robot_type

visualize:
  dataset_name: nero_task3_step1/empty_merged_E113_cleaned_mirrored
  episode_index: 0 # episode index to visualize
